/**
 * Database schema definitions live here. Tables land in feature PRs
 * (starting with `feat/db-schema`) — see open PRs for current state.
 *
 * Re-exports below let `import * as schema from './schema'` keep working as
 * tables are added over time.
 */
export {};
